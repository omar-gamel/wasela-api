# Migration `20200301191755`

This migration has been generated by omar-gamel at 3/1/2020, 7:17:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."_CourseToPath" (
    "A" text  NOT NULL ,
    "B" text  NOT NULL 
) 

ALTER TABLE "public"."Course" DROP COLUMN "path";

CREATE UNIQUE INDEX "_CourseToPath_AB_unique" ON "public"."_CourseToPath"("A","B")

ALTER TABLE "public"."_CourseToPath" ADD FOREIGN KEY ("A") REFERENCES "public"."Course"("id") ON DELETE CASCADE

ALTER TABLE "public"."_CourseToPath" ADD FOREIGN KEY ("B") REFERENCES "public"."Path"("id") ON DELETE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200301190634..20200301191755
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://postgres:root@localhost:5432/mydb?schema=public"
 }
 generator client {
   provider = "prisma-client-js"
@@ -112,8 +112,9 @@
   days          Day[]
   instructor    User
   category      Category
   review        Review[]
+  paths         Path[]
   createdAt     DateTime           @default(now())
   updatedAt     DateTime           @updatedAt
 }
@@ -148,14 +149,12 @@
   description String
   attachments String[]
   lectures    Lecture[]
   assignments Assignment[]
-
-  comments Comment[]
-  reviews  DayReview[]
-
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
+  comments    Comment[]
+  reviews     DayReview[]
+  createdAt   DateTime     @default(now())
+  updatedAt   DateTime     @updatedAt
 }
 model Lecture {
   id        String   @id @default(cuid())
```


